---
with-expecter: True
filename: "{{ .InterfaceName | snakecase }}.go"
outpkg: "mock_{{.PackageName}}"
packages:
  github.com/Zampfi/workflow-sdk-go/sdk:
    config:
      dir: "mocks/{{.InterfaceDirRelative}}"
      all: True
      recursive: True
